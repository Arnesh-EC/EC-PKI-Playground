# --- Python (backend/) ---
__pycache__/
*.pyc
.venv/
*.egg-info/
*.iso

# pki-orchestrator agent build artifact — a per-host binary pointed at by
# ORCHESTRATOR_AGENT_PATH, not vendored in the repo.
backend/agent/pki-orchestrator.exe

# --- Node / Vite (frontend/) ---
node_modules/
dist/
dist-ssr/
*.local

# --- Env / editor ---
.env
.env.*
!.env.example
.DS_Store
*.log
